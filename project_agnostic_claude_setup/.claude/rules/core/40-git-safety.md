# Git Safety

- Stage explicit files, not everything.
- Prefer small reversible commits.
- Do not use `git add .`, `git add -A`, `--no-verify`, force-push, `reset --hard`, `clean`, `restore`, or `checkout` rewrites without explicit approval.
- Do not amend commits unless the user asks.
- Do not overwrite unrelated user changes.
